version: '3.9'

services:
  ecc-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_APP_NAME=Executive Command Center
    restart: unless-stopped

  # Uncomment for full stack with PostgreSQL:
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: ecc_db
  #     POSTGRES_USER: ecc_user
  #     POSTGRES_PASSWORD: changeme
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"

# volumes:
#   postgres_data:
